plugins {
  id "com.moowork.node" version "1.1.1"
}

task buildProd(type: NpmTask) {
  args = ['run', 'build:prod']
}

task clean(type: Delete) {
  delete "../ucl-hackathon-ui/public"
  followSymlinks = true
}

//task build(type: NpmTask) {
//  args = ['run', 'build:prod']
//}
